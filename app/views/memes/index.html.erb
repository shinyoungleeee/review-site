<div class="row">
  <h1>Memes</h1>
</div>

<div class="row">
  <ul>
    <% @memes.each do |meme| %>
      <li><%= link_to meme.name %></li>
      <li><img src='<%= meme.image_url %>'></li>
    <% end %>
  </ul>

  <h5><%= link_to 'Add New Meme!', new_meme_path %></h5>
</div>
